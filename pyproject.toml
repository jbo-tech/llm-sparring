[project]
name = "llm-sparring"
version = "0.1.0"
description = "MCP server for multi-LLM sparring sessions"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.25.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
